# ======================
# 数据库配置 (推荐使用 Render Environment Group)
# ======================
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require
PGDATABASE=your_database_name
PGHOST=your_database_host
PGPORT=5432
PGUSER=your_database_user
PGPASSWORD=your_database_password

# ======================
# JWT 认证配置 (必需)
# ======================
JWT_SECRET=your-super-secure-jwt-secret-min-32-characters
JWT_EXPIRES_IN=7d

# ======================
# 应用配置
# ======================
# 注意：在 Render 上不要设置 PORT，让 Render 自动分配
# PORT=5000  # 仅本地开发使用
NODE_ENV=production

# ======================
# 安全配置
# ======================
BCRYPT_ROUNDS=12

# ======================
# CORS 配置 (生产环境必需)
# ======================
ALLOWED_ORIGINS=https://your-frontend-service.onrender.com,https://your-admin-panel.onrender.com